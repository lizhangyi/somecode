# 2026-07-22 工作日志

## 桌面宠物项目（desktop-pet）

- 新建项目 `projects/desktop-pet/`，基于章毅的虎斑猫照片开发桌面宠物
- 技术栈：Python 3.13 + PySide6 (Qt6) + pynput + Pillow（客户明确要求不用前端技术）
- 走标准 SOP：产品经理(PRD) → 架构师(设计+任务分解) → 工程师(代码实现) → QA(测试)
- PRD 272行，架构设计 492行，源码 18 个 .py 文件共 3150 行，测试 91 用例全部通过
- 工程师在 50 轮限制内写完了全部代码但卡在审查输出阶段超时，主理人补做了一致性审查（IS_PASS: YES）
- QA 两轮测试：第1轮发现1个测试代码隔离缺陷（QA自行修复），第2轮回归全过，零源码 Bug
- 精灵图用 Pillow ImageDraw 程序化绘制 9 种状态共 37 帧，缓存到 assets/sprites/
- 功能：透明置顶窗口、拖拽、点击互动、自动行为状态机、键鼠计数、道具奖励、喂食、托盘、设置面板
- venv 路径：`C:\Users\lizhangyi\.workbuddy\binaries\python\envs\desktop-pet\`
- 启动命令：`cd src && python main.py`
- PyInstaller 打包完成（onedir 模式）：exe 在 `dist/桌面宠物/桌面宠物.exe`，整个目录 107MB
- 源码改动：constants.py 新增 resource_path()/sys._MEIPASS 适配，分离只读资源与可写数据路径
- 新增 build.spec（PyInstaller 配置）和 assets/icon.ico（猫脸图标）
- 分发方式：将 dist/桌面宠物/ 整个目录打包为 zip，解压后双击 exe 运行

## 20:43-20:50 方案 A：AI 素材 + 程序微动画
- 用 ImageGen 生成 9 张卡通萌系虎斑猫 PNG（assets/ai_sprites/idle.png 等），透明背景 1024×1024
- 自己重写 sprite_generator.py：从 AI 目录加载单张 PNG，删除 Pillow 绘制逻辑
- 修改 pet_renderer.py：FRAME_COUNTS 驱动 Qt 变换矩阵（平移/旋转/缩放/翻转），实现呼吸/摇摆/翻转/弹跳等微动画
- 更新 constants.py：新增 AI_SPRITES_DIR，缓存版本升为 v2-ai
- 更新 tests/test_sprite_generator.py：单帧 1024×1024、总帧数 9
- 测试：92/92 全通过；app offscreen 模式运行 2s 无崩溃
- 生成 assets/ai_sprites/animation_preview.png 展示 9 状态微动画效果
- 说明：工程师 agent 两次在读取阶段耗尽轮次未产出代码，后续改为我自己直接修改以节省 credits
