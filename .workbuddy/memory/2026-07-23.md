# 2026-07-23 工作日志

## GraphEditor 关系图编辑器组件完整实现

- 在 `projects/graph-editor/` 创建了完整的 Vue 3 + TypeScript + AntV G6 4.8.x 关系图编辑器组件
- 架构：三 composable 模式 —— useGraphInstance（G6 实例管理）、useCommandManager（命令模式/撤销重做）、useGraphSync（增量保存/防抖/重试/冲突处理）
- 实现功能：
  - P0：节点/边增删改、Shift+拖拽连线、三种边类型切换、力导向布局+拖拽固定、编辑/展示双模式、StorageAdapter 增量保存、FitView/导出PNG
  - P1：撤销/重做（Ctrl+Z/Y）、快捷键（Delete/Ctrl+A/Esc）、框选、脏检查（beforeunload）、保存失败重试/409冲突处理、Minimap缩略图
  - P2：缩放控件、#loading/#empty 插槽、未保存指示器
- Playground 页面（GraphDemo.vue）：localStorage StorageAdapter、模式切换、节点属性侧边栏编辑、工具栏、预设3节点2边演示数据
- 单元测试：useCommandManager.spec.ts（10个测试全部通过）
- TypeScript strict 编译零错误，Vitest 测试全部通过
- 开发服务器：`cd projects/graph-editor && npm run dev` → http://localhost:5174
- Bug 修复：initGraph 中 loading 状态导致 canvas container display:none，G6 以 0x0 尺寸创建 → 修复为在测量容器尺寸前关闭 loading
- Bug 修复：renderData 缺少 graph.render() 调用，G6 4.x 需 data() + render() 才会绘制
- Bug 修复：useGraphSync watch deep:false 导致 push 不触发保存 → 改为 deep:true
- Bug 修复：保存成功后 currentVersion 未同步导致 409 冲突 → onSaved 回调中 +1
- Bug 修复：拖拽节点不更新存储 → 将 node:dragend 处理从 useGraphInstance 移到 bindGraphEvents，统一做 model.fx/fy 写入 + UpdateNodeCommand.execute()，使拖拽位置变化进入 operationQueue 触发 useGraphSync 保存
