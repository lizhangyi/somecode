# WordMaster

基于 SM-2 间隔重复算法的 PWA 背单词应用。

## 技术栈

- 纯原生 HTML/CSS/JS，零依赖
- IndexedDB 本地持久化
- Service Worker 离线支持
- PWA Manifest（可添加到主屏幕）

## 运行

```bash
# 在项目根目录下启动
python -m http.server 8080
# 访问 http://localhost:8080/projects/wordmaster/
```

## 词库

内置 210 个雅思 + 六级高频词汇。
